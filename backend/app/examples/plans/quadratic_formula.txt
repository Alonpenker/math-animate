{
  "scenes": [
    {
      "scene_number": 1,
      "learning_objective": "Identify the coefficients of a quadratic, connect them to the general form, and substitute them into the quadratic formula setup.",
      "visual_storyboard": "Keep the title 'Set Up the Quadratic Formula' at the top. Use the standard title, middle, and bottom zones from MANIM.md. In the middle zone, place the concrete equation 2x^2 - 7x + 3 = 0 near the upper-middle. Below it, write the general form ax^2 + bx + c = 0 and use three consistent highlight roles to distinguish a, b, and c. Beneath that, write the coefficient-identification line a = 2, b = -7, c = 3 using the same three highlight roles so the viewer can track the mapping from general form to concrete equation. In this first middle layout, stack the concrete equation, general form, and coefficient-identification line vertically with generous spacing so each mapping layer is readable. Then clear the middle section completely. In the second middle layout, place the quadratic formula near the upper-middle and keep the same three highlight roles on a, b, and c inside the formula. Below it, derive the fully substituted version with -7, 2, and 3 inserted into the formula. Replace that substituted line with the simplified setup line where -(-7) has been rewritten as 7 while the rest of the expression stays in setup form. Finish by copying that simplified setup line to the bottom as the scene takeaway. Keep the concrete starting equation in default styling, use the three coordinated highlight roles to track a, b, and c across the general form, the coefficient-identification line, the symbolic formula, and the substituted setup, and reserve final-answer styling for the copied bottom setup line only. Treat the scene as two phases: coefficient identification, then formula setup. The bottom statement should copy the simplified substituted setup rather than restating the original equation or jumping ahead to the solutions.",
      "voice_notes": "Identify a, b, and c from the quadratic, carry those values into the formula, and simplify the leading negative on b so the setup becomes ready for evaluation."
    },
    {
      "scene_number": 2,
      "learning_objective": "Compute the discriminant step by step and show the resulting square root information used by the formula.",
      "visual_storyboard": "Keep the title 'Compute the Discriminant' at the top. In the middle zone, place D = b^2 - 4ac near the upper-middle. Replace it in place with the substituted expression D = (-7)^2 - 4 * 2 * 3. Replace that with D = 49 - 24, and then replace that with D = 25. Keep the main discriminant line fixed near the upper-middle while it is rewritten in place. Below the final discriminant value, write sqrt(25) = 5. Below that, add a short math line stating D > 0 and sqrt(D) = 5. Stack the square-root line and the final interpretation line beneath the computed discriminant with enough vertical spacing to keep three active lines readable at once. Use default styling for the discriminant rewrites and supporting math lines, and reserve final-answer styling for the copied bottom discriminant value only. Do not introduce diagrams or prose labels; keep the interpretation as math-only content beneath the computed discriminant. Finish by copying D = 25 to the bottom as the takeaway for the scene.",
      "voice_notes": "Substitute into the discriminant, evaluate each arithmetic part, simplify to 25, and note that its square root is 5."
    },
    {
      "scene_number": 3,
      "learning_objective": "Use the plus-minus structure of the simplified quadratic formula setup to obtain both solutions and collect them into a set.",
      "visual_storyboard": "Keep the title 'Use the Plus-Minus for Both Solutions' at the top. In the middle zone, place the simplified formula x = (7 +/- 5) / 4 near the upper-middle. Below it, derive the plus case as a full line ending in 3. Beneath that, derive the minus case as a full line ending in 1/2. Place the plus case directly beneath the simplified formula and the minus case beneath the plus case with generous spacing so the two branches read as separate outcomes of the same source line. Below both case lines, derive the solution set containing 3 and 1/2 from the two case results. Then move the solution set to the bottom by replacing it with a bottom conclusion, while keeping the top simplified formula and both worked case lines visible in the middle. Use default styling for the simplified formula and both case computations, and reserve final-answer styling for the bottom solution set only. Derive the plus and minus cases from the shared simplified formula, then derive the set notation from the two case lines. The bottom set should replace the middle set, not appear as an unrelated extra object.",
      "voice_notes": "Split the plus-minus into two cases, evaluate each case completely, and combine the two results into the final solution set."
    }
  ]
}
