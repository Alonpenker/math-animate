{
  "scenes": [
    {
      "scene_number": 1,
      "learning_objective": "Factor a quadratic by matching the roles of a, b, and c to a concrete example and then checking candidate number pairs against the product-and-sum conditions.",
      "visual_storyboard": "Keep the title 'Factor ax^2 + bx + c' at the top. Use the three-zone layout from MANIM.md. In the middle zone, place the general form ax^2 + bx + c near the upper-middle and the example x^2 + 5x + 6 directly beneath it. Replace both with equivalent versions that introduce three distinct highlight roles for the parameters in the general form and for the corresponding visible values in the example. Because the example has an implicit coefficient of 1, make that role explicit later in the mapping block rather than inventing a visible 1 in the example expression. Then add a vertical parameter-mapping block below the example that states a = 1, b = 5, and c = 6 using the same three highlight roles. Below that, show a two-line rule block: one line for the product condition r1 r2 = a * c and one line for the sum condition r1 + r2 = b. Replace that rule block with the evaluated version using the example values. These four middle layers stay active together until the rule block is evaluated. After the value block is shown, clear the middle section completely. In the new middle layout, show two candidate pair checks as a vertical block centered in the middle: first a rejected pair for 6 that fails the sum condition, then an accepted pair that satisfies both product and sum. Give the rejected pair a dedicated incorrect-treatment style and the accepted pair a separate positive-selection style. Place the factorization (x + 2)(x + 3) beneath the pair-check block and derive it from the accepted pair. Finish by copying that factorization into a bottom statement that shows the full quadratic equals its factored form, treating only the full bottom equation as the final-answer content. Use the three coordinated highlight roles to track a, b, and c through the general form, the mapping block, and the evaluated rule block, and derive the bottom conclusion from the factored expression rather than introducing it independently.",
      "voice_notes": "Identify the roles of a, b, and c, compute the product target and sum target, test candidate pairs, reject the one that fails, accept the one that satisfies both conditions, and write the resulting factorization."
    }
  ]
}
