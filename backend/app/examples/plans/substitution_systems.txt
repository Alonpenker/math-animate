{
  "scenes": [
    {
      "scene_number": 1,
      "learning_objective": "Use substitution to turn a two-equation system into a single equation in one variable.",
      "visual_storyboard": "Keep the title 'Solve by Substitution' at the top. Use the standard title, middle, and bottom zones from MANIM.md. In the middle zone, show the system as a vertical two-line block centered around the upper-middle: y = 2x + 1 on top and x + y = 7 below, with enough vertical spacing between the lines to make the substitution path easy to read. Replace that block with an equivalent version where the y in both equations is highlighted with the same linkage treatment so the viewer can see exactly which symbol is being tracked across the system. Then replace the highlighted block with a new two-line block where the second equation becomes x + (2x + 1) = 7 while the first equation remains y = 2x + 1. Replace that full block with the simplified single equation 3x + 1 = 7 centered in the same middle area. Finish by copying that simplified equation to the bottom as the takeaway for the scene. Use default styling for the initial system and the substituted block, one shared linkage highlight for the matching y terms across both lines, and final-answer styling for the copied bottom takeaway only. Only one middle block is active at a time: the original system, then the highlighted system, then the substituted system, then the simplified single equation. Make the substitution feel like a full-block rewrite rather than a partial edit. The bottom statement should be a direct copy of the simplified middle equation, not a new algebra step.",
      "voice_notes": "Start from the system, identify that y is already isolated, substitute that expression into the second equation, and combine like terms to reach 3x + 1 = 7."
    },
    {
      "scene_number": 2,
      "learning_objective": "Finish solving the substituted equation, back-substitute into the original expression for y, and present the ordered-pair solution.",
      "visual_storyboard": "Keep the title 'Finish and Solve' at the top. In the middle zone, place 3x + 1 = 7 near the upper-middle. Show a same-operation cue to the right for subtracting 1, styling the cue as an annotation distinct from the equation, then replace the equation with 3x = 6 while removing the cue. Show a second right-side cue for dividing by 3, then replace the equation with x = 2 while removing that cue. Keep the solved-x line centered near the upper-middle. Next, write y = 2x + 1 below x = 2 with enough vertical spacing to read both lines as a single working stack. Replace the pair of lines with a linked version where the x in the top line and the x in the lower equation share the same highlight treatment, so the viewer can infer the back-substitution connection. Then replace only the lower equation with y = 2(2) + 1, and finally replace that lower line with y = 5 while keeping x = 2 visible above it. Finish by copying the visible x-value line and y-value line together into the bottom ordered pair (2, 5). Use default styling for valid equations, annotation styling for the right-side operation cues, one shared linkage highlight for the x terms during back-substitution, and final-answer styling for the bottom ordered pair only. During the back-substitution stage, the active middle content is always a two-line stack: x = 2 on top and the current y line below. Use the cue-plus-rewrite pattern for the equation solving, then switch to a stacked two-line layout for back-substitution. The final ordered pair should be derived from the visible x and y results rather than introduced independently.",
      "voice_notes": "Solve 3x + 1 = 7 by subtracting 1 and dividing by 3, then substitute x = 2 back into y = 2x + 1 to get y = 5. The solution is the ordered pair (2, 5)."
    }
  ]
}
