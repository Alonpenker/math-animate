{
  "scenes": [
    {
      "scene_number": 1,
      "learning_objective": "Apply PEMDAS step-by-step to evaluate two expressions, tracking which operation is active using a checklist panel.",
      "visual_storyboard": "Display the title 'Order of Operations' at the top. On the right side of the screen, show a vertical PEMDAS checklist panel (RoundedRectangle) with four rows: Parentheses, Exponents, Multiply, Add/Subtract — each with an empty checkbox. On the left, write the first expression 4 + 2(5 − x) with x = 3 shown above it in teal. Animate a value-3 label flying into the x position, then use TransformMatchingTex to substitute. Highlight the parentheses group in yellow, color the matching step label yellow, simplify (5−3) to 2, then show the green checkmark in the checkbox. Shift the highlight to the multiplication group in teal, simplify 2(2) to 4, check that box. Shift to the addition group in orange, simplify 4+4 to 8 in green, check that box. Fade out and repeat with the second expression 3(2+x)^2 with x = 1, cycling through Parentheses (3+1→3), Exponents (3^2→9), and Multiply (3·9→27), checking each box.",
      "voice_notes": "PEMDAS tells us the order to evaluate operations. First, parentheses. Then exponents. Then multiplication and division, then addition and subtraction. In the first example, 4 plus 2 times the quantity 5 minus x with x equal to 3 — we subtract inside the parentheses first to get 2, then multiply 2 by 2 to get 4, then add 4 plus 4 to get 8. In the second example, 3 times the quantity 2 plus 1 squared — add inside the parentheses first, square the result, then multiply.",
      "template_hints": "Build the PEMDAS panel as a RoundedRectangle with step_row helper: each row has a Square checkbox, a hidden checkmark Text, and a label Text. Use set_color to activate each step label and FadeIn to reveal the checkmark. Use SurroundingRectangle with ReplacementTransform to move the highlight box from step to step. Use animate_substitution to fly the value label into the x position before TransformMatchingTex."
    }
  ]
}
