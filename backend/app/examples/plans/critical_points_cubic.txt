{
  "scenes": [
    {
      "scene_number": 1,
      "learning_objective": "Differentiate a cubic, set the derivative equal to zero, and simplify the resulting quadratic equation used to find critical x-values.",
      "visual_storyboard": "Keep the title 'Find Critical x-Values' at the top. Use the standard title, middle, and bottom zones from MANIM.md. In the middle zone, place the critical-value rule near the upper-middle as the persistent reference. Below it, write f(x) = x^3 - 6x^2 + 9x. Replace that function line with the derivative line f'(x) = 3x^2 - 12x + 9, using a focused instructional highlight treatment so the derivative reads as the active result. Replace the derivative line with 3x^2 - 12x + 9 = 0. Then show a right-side operation cue indicating division by 3, with the cue clearly separated from the equation as an annotation distinct from the algebra line. Keep the active algebra line directly beneath the rule and keep all rewrites in that same position. The divide cue should sit to the right of the active equation with enough horizontal room to avoid overlap. Replace the equation-and-cue block with the simplified equation x^2 - 4x + 3 = 0 in the same position. Use default styling for the rule and the original function line, a focused instructional highlight treatment for the derivative line, annotation styling for the right-side divide cue, and final-answer styling for the copied bottom quadratic only. Treat the divide-by-3 note as a temporary operation cue beside the equation rather than part of the equation itself. Finish by copying that simplified equation to the bottom as the scene takeaway while keeping the rule and simplified middle equation visible.",
      "voice_notes": "State the critical-value rule, differentiate the cubic, set the derivative equal to zero, and simplify the equation by dividing through by 3."
    },
    {
      "scene_number": 2,
      "learning_objective": "Factor the quadratic for the critical x-values and then place those x-values on a number line.",
      "visual_storyboard": "Keep the title 'Solve for the Critical x-Values' at the top. In the middle zone, place x^2 - 4x + 3 = 0 near the upper-middle. Replace it with the factored form (x - 1)(x - 3) = 0 using a focused instructional highlight treatment so the factoring step stands out. Below that, derive a two-item horizontal solution block showing x = 1 and x = 3. Below the solutions, create a number line. Position the number line below the solutions with enough vertical spacing that both labeled points can sit under the line without colliding with the bottom section. Add the point-and-label block for x = 1 first, then add the point-and-label block for x = 3. Use default styling for the starting quadratic, the number line, the solution labels, and the number-line point labels, and reserve final-answer styling for the copied bottom solution list only. The middle remains cumulative in this scene: factored equation, solution block, number line, then the two labeled points. Derive the solutions from the factored equation first, then use those same values as the source for the number-line markers and the bottom conclusion. Finish by copying the solution block to the bottom as the scene conclusion.",
      "voice_notes": "Factor the quadratic, read off x = 1 and x = 3, and then show those values on a number line."
    },
    {
      "scene_number": 3,
      "learning_objective": "Turn the critical x-values into coordinate points by evaluating the function and then locating those points on the graph.",
      "visual_storyboard": "Keep the title 'Turn Critical x-Values into Points' at the top. In the middle zone, place f(x) = x^3 - 6x^2 + 9x near the upper-middle. Below it, write the two x-values x = 1 and x = 3 as a horizontal block. Beneath that, derive f(1) = 1 - 6 + 9 = 4 and then f(3) = 27 - 54 + 27 = 0, using a focused instructional highlight treatment for these evaluation lines so the function-value calculations read as active derivations. In the first half of the scene, stack the function, x-value block, and two evaluation lines vertically with enough spacing to keep all four lines readable at once. Then clear the middle section completely. In the second half, create a coordinate plane centered in the middle, draw the cubic curve, and keep the curve fully inside the middle bounds. Add the labeled point block for (1, 4) with its label above the point. After that, add the labeled point block for (3, 0) with its label below the point. Use default styling for the original function, x-value block, plane, curve, and point labels, and reserve final-answer styling for the copied bottom point list only. Do not keep the algebra stack visible once the graph appears; treat the graphing portion as a fresh middle layout derived from the earlier evaluations. Finish by copying the two labeled graph points into the bottom conclusion (1, 4), (3, 0).",
      "voice_notes": "Evaluate the function at x = 1 and x = 3, then use those results to place the points (1, 4) and (3, 0) on the graph."
    }
  ]
}
