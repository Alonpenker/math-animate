{
  "scenes": [
    {
      "scene_number": 1,
      "learning_objective": "Introduce the unsorted list and explain what bubble sort aims to achieve.",
      "visual_storyboard": "Render eight Rectangle mobjects side by side, with heights proportional to the values [5, 3, 8, 1, 4, 7, 2, 6]. Place a Text label showing the numeric value at the center of each bar. Animate the bars growing upward from the baseline using GrowFromEdge. Add a title 'Bubble Sort' at the top using Write.",
      "voice_notes": "We start with an unsorted list of eight numbers. Bubble sort works by repeatedly stepping through the list, comparing adjacent elements, and swapping them when they are in the wrong order.",
      "template_hints": "Use a VGroup of Rectangle + Text pairs. Align bars along a common baseline using align_to."
    },
    {
      "scene_number": 2,
      "learning_objective": "Show the comparison and swap steps of one full pass through the list.",
      "visual_storyboard": "Iterate adjacent pairs. For each comparison, highlight both bars yellow using animate.set_fill. If a swap is needed, animate the two bars exchanging x-positions with a simultaneous shift animation over 0.4 seconds, then color the bar that moved into correct position green. If no swap is needed, restore the original blue color. Repeat for all pairs in the pass.",
      "voice_notes": "In each pass, we compare neighboring bars. When the left bar is taller than the right, we swap them. After each complete pass, the largest unsorted value has bubbled up to its correct position.",
      "template_hints": "Track bar positions explicitly. Use bar.animate.shift to move each bar, updating the internal position list after every swap."
    },
    {
      "scene_number": 3,
      "learning_objective": "Show the fully sorted list and summarize the algorithm's outcome.",
      "visual_storyboard": "After all passes complete with no swaps, flash all bars green simultaneously using LaggedStart with Flash. Display a 'Sorted!' Text label at the top of the screen with Write. Hold the final frame for two seconds, then fade everything out with FadeOut.",
      "voice_notes": "Once a full pass produces no swaps, the list is sorted. Every bar is now in its correct position, and the animation ends.",
      "template_hints": null
    }
  ]
}
