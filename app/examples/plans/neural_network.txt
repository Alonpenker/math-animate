{
  "scenes": [
    {
      "scene_number": 1,
      "learning_objective": "Show the static architecture of a feedforward neural network with three layers.",
      "visual_storyboard": "Draw three vertical columns of Circle mobjects: 4 input nodes on the left, 5 hidden nodes in the center, 2 output nodes on the right. Space nodes 0.8 units apart vertically and 3 units apart horizontally. Connect every input node to every hidden node, and every hidden node to every output node, using Line mobjects in light gray. Animate nodes appearing with LaggedStart and Create, then connections fading in with FadeIn.",
      "voice_notes": "This is a simple feedforward neural network. It has an input layer, one hidden layer, and an output layer. Each node in one layer connects to every node in the next.",
      "template_hints": "Store nodes in a list of VGroups per layer. Draw connections by iterating over all pairs between adjacent layers."
    },
    {
      "scene_number": 2,
      "learning_objective": "Animate the forward pass from the input layer through to the hidden layer.",
      "visual_storyboard": "Place small DecimalNumber labels inside the input circles showing example input values. For each connection from an input node to a hidden node, animate the Line color changing to yellow in sequence using LaggedStart. Once all connections to a hidden node are highlighted, flash the hidden node yellow with Flash, then display its sigmoid activation value using a DecimalNumber that counts up to the computed result.",
      "voice_notes": "During the forward pass, each input value travels along the connections to the hidden layer. Each hidden node computes a weighted sum and applies a sigmoid activation function.",
      "template_hints": "Use ValueTracker and always-redraw DecimalNumber to animate the activation value counting up. Highlight connections with set_color inside a LaggedStart."
    },
    {
      "scene_number": 3,
      "learning_objective": "Complete the forward pass to the output layer and highlight the predicted class.",
      "visual_storyboard": "Repeat the connection highlighting from hidden to output layer. Color each output node on a blue-to-red gradient based on its activation value. Highlight the output node with the highest activation in green using a surrounding circle and Flash. Display a 'Prediction: Class 1' Text label beside it. Fade the rest of the network to 20% opacity over 1.5 seconds using animate.set_opacity.",
      "voice_notes": "The activations propagate to the output layer. The node with the highest activation determines the network's prediction. In this case, the network predicts Class 1.",
      "template_hints": null
    }
  ]
}
