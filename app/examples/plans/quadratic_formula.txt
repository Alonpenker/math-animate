{
  "scenes": [
    {
      "scene_number": 1,
      "learning_objective": "Identify a, b, and c in a quadratic equation and substitute them into the quadratic formula.",
      "visual_storyboard": "Show the title 'Set Up the Quadratic Formula'. Write 2x² − 7x + 3 = 0 in large font near the top. Sequentially create colored SurroundingRectangles around each coefficient: a (red around 2x²), b (blue around −7x), c (green around 3), each time fading in the matching label a=2, b=−7, c=3 to the right. Write the quadratic formula x = (−b ± √(b²−4ac)) / 2a below the equation. Animate colored value copies flying from each label into the corresponding positions in the formula (a targets, b targets, c targets move simultaneously with Indicate flashes on the destinations). Then use TransformMatchingTex to replace the formula with the fully substituted version x = (−(−7) ± √((−7)²−4·2·3)) / (2·2) in matching colors. Add a yellow note 'Be careful with the negative b value' below.",
      "voice_notes": "The quadratic formula works for any quadratic ax squared plus bx plus c equals zero. First we read off a, b, and c: a equals 2, b equals negative 7, c equals 3. Then we substitute: x equals negative b, which is negative negative 7, plus or minus the square root of b squared minus 4ac, all over 2a. Pay close attention to the sign of b when substituting.",
      "template_hints": "Color-code a, b, c with distinct colors (A_COLOR=red, B_COLOR=blue, C_COLOR=green) applied via set_color_by_tex. Use get_parts_by_tex to locate all a, b, c occurrences in the formula, then animate flying copies from the label values to each target position simultaneously. Use TransformMatchingTex for the final substitution step."
    },
    {
      "scene_number": 2,
      "learning_objective": "Compute the discriminant b² − 4ac and interpret its value to determine the nature of the solutions.",
      "visual_storyboard": "Show the title 'Compute the Discriminant' with a helper label 'D = b² − 4ac' in yellow. Write the discriminant expression D = (−7)² − 4·2·3, coloring the b part blue and the 4ac part red. Below it, show two side-by-side computation steps: (−7)² = 49 on the left (blue result) and 4·2·3 = 24 on the right (red result), each boxed with a SurroundingRectangle. Copy the results 49 and 24 into a simplified D = 49 − 24 expression using TransformFromCopy. Remove the side steps, then use TransformMatchingTex to collapse to D = 25 in yellow. Below, show √25 = 5 in green and a note 'Perfect square → two real rational solutions'.",
      "voice_notes": "The discriminant is b squared minus 4ac. With b equals negative 7, b squared is 49. With a equals 2 and c equals 3, 4 times 2 times 3 equals 24. So D equals 49 minus 24, which equals 25. The square root of 25 is 5, a whole number, which tells us the equation has two real, rational solutions.",
      "template_hints": "Display the full discriminant expression with index-based coloring for the b-squared part and the 4ac part. Position the two side computations in lower left and lower right. Use SurroundingRectangle around each final result and TransformFromCopy to pull those values into the D = 49 − 24 step before collapsing."
    },
    {
      "scene_number": 3,
      "learning_objective": "Split the ± into two cases and simplify each to find both solutions of the quadratic.",
      "visual_storyboard": "Show the title 'Use the Plus-Minus for Both Solutions' with the helper 'One formula, two cases' in yellow. Write the compact formula x = (7 ± 5) / 4 centered near the top. Create two downward Arrow mobjects diverging to the lower left (blue) and lower right (orange). On the left, show 'Plus case' in blue followed by x₁ = (7+5)/4 → x₁ = 12/4 → x₁ = 3 stacked vertically using TransformMatchingTex for each step. Simultaneously on the right, show 'Minus case' in orange with x₂ = (7−5)/4 → x₂ = 2/4 → x₂ = 1/2. Once both reach their final values in green, fade out the intermediate steps and arrows, then use TransformMatchingTex to merge x₁=3 and x₂=1/2 into the solution set x ∈ {3, 1/2} in green with a 'Solution set' label above.",
      "voice_notes": "With negative b equal to 7 and square root of D equal to 5, the formula gives x equals 7 plus or minus 5, all over 4. The plus case: 7 plus 5 is 12, divided by 4 gives x₁ equals 3. The minus case: 7 minus 5 is 2, divided by 4 gives x₂ equals one-half. Our solution set is {3, 1/2}.",
      "template_hints": "Create plus_track and minus_track as VGroups each containing a label Text and three MathTex steps. Use Write for the first fraction step and TransformMatchingTex for each subsequent simplification. Use Arrow mobjects pointing from the compact formula down to each track. Use TransformMatchingTex with a combined VGroup of the two final values to morph into the set notation."
    }
  ]
}
