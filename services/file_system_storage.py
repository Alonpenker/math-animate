from uuid import UUID


class FileSystemStorage:
    #TODO: should access the MiniIO and save and retrieve files
    
    @staticmethod
    def save_artifact(job_id: UUID, code: str) -> None:
        pass
